{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/*"]
    },
    "dev": {
      "outputs": ["dist/*"]
    },
    "test": {}
  },
  "globalDependencies": [
    "tsconfig.json"
  ]
}
