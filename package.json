{
  "private": true,
  "scripts": {
    "docs:build": "typedoc --tsconfig tsconfig.json",
    "build": "pnpm run build -r --if-present",
    "lint": "pnpm run lint -r --if-present",
    "test": "pnpm run test -r --if-present"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "@m9ch/eslint-config-all": "^0.1.1",
    "@types/node": "*",
    "eslint": "^8.11.0",
    "tsup": "^5.12.1",
    "typedoc": "^0.22.15",
    "typescript": "^4.6.2"
  }
}
